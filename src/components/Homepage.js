import React from 'react';
import Hero from './Hero';
import Products from './Products';

const Homepage = () => {
    return (
        <main>
            <Hero />
            <Products />
        </main>
    );
};

export default Homepage;
